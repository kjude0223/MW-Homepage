<section class="lane-strip" id="lanes">
  <div class="lane-strip-inner">
    <a href="#builds" class="lane-item">
      <span class="lane-build-tag">Active</span>
      <span class="lane-name">Import</span>
      <span class="lane-desc">JDM, Euro, and every import that actually gets driven. The lane this brand was built in.</span>
    </a>
    <a href="/pages/about" class="lane-item">
      <span class="lane-build-tag">Open</span>
      <span class="lane-name">Every Lane</span>
      <span class="lane-desc">Truck, muscle, exotic — if you built it and you drive it, this lane is yours. Submit your build.</span>
    </a>
  </div>
</section>

{% schema %}
{
  "name": "Lane Strip",
  "settings": [],
  "presets": [{ "name": "Lane Strip" }]
}
{% endschema %}
